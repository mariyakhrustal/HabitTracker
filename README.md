# Проект - HabitTracker
Приложение развернуто и доступно по IP: `94.131.86.4`.

## Описание

Бэкенд-часть SPA-приложения для трекинга полезных привычек, вдохновлённая книгой Джеймса Клира "Атомные привычки".
Проект реализует API для управления полезными и приятными привычками, с поддержкой аутентификации, прав доступа, 
пагинации и валидации бизнес-логики. Напоминания о привычках отправляются через Telegram с использованием отложенных задач Celery.
Проект полностью контейнеризирован с помощью Docker и развернут на сервере.

## Требования

* Python 3.12 / Django / DRF
* PostgreSQL (База данных)
* Redis (Брокер сообщений)
* Celery & Celery Beat (Фоновые и периодические задачи)
* Docker & Docker Compose (Контейнеризация)

## Установка:

- Клонируйте репозиторий:

```
git clone git@github.com:mariyakhrustal/HabitTracker.git
```

- Установите зависимости:

```
pip install -r requirements.txt
```

## Инструкция по запуску

### 1. Настройка переменных окружения

Создайте в корне проекта файл `.env` и заполните его, используя в качестве примера файл `.env_sample`.

**Обратите внимание на настройки для Docker:**

* `CELERY_BROKER_URL=redis://redis:6379/0`
* `CELERY_RESULT_BACKEND=redis://redis:6379/0`

### 2. Сборка и запуск контейнеров

Запустите проект одной командой:

```
docker-compose up --build -d
```

Эта команда соберет образы и запустит все сервисы в фоновом режиме.
После запуска веб-приложение будет доступно по адресу: `http://localhost:8000`.

### 3. Проверка работоспособности сервисов

Для проверки статуса всех контейнеров выполните:

```
docker-compose ps
```

### 4. Для просмотра логов всех контейнеров

```
docker-compose logs
```

### 5. Для остановки сервисов и удаления контейнеров:

```
docker-compose down
```

## Настройка удаленного сервера и деплой

В проекте настроен автоматический деплой через GitHub Actions. При каждом push и pull request запускается цепочка (Workflow),
которая проверяет код через линтеры, тестирует, собирает и обновляет проект на сервере.

### Настройка удаленного сервера

Для успешного деплоя на сервере должны быть:

- ОС: Ubuntu.
- Docker и Docker Compose.
- Добавьте ваш публичный SSH-ключ в ~/.ssh/authorized_keys на сервере.
- Убедитесь, что порты 80, 8000 и 22 открыты в файрволе
  (Для проверки открытых портов на сервере воспользуйтесь командой `sudo ufw status`).
- Созданная папка проекта (путь по умолчанию: ~/myapp).

### Настройка GitHub Secrets

Для работы Workflow необходимо добавить следующие переменные в Settings -> Secrets and variables -> Actions:

* SSH_KEY - Ваш приватный SSH ключ для доступа к серверу
* SERVER_IP - IP адрес вашего сервера
* SSH_USER - Имя пользователя на сервере (например, root)
* DOCKER_HUB_USERNAME - Логин на Docker Hub
* DOCKER_HUB_ACCESS_TOKEN - Токен доступа Docker Hub
* SECRET_KEY - Django Secret Key
* DB_NAME / DB_USER / DB_PASSWORD - Данные для подключения к PostgreSQL

### Процесс деплоя (Workflow)

Автоматизация разделена на 4 этапа:

1. Lint: Запуск линтеров на проверку кода.
2. Test: Запуск тестов Django.
3. Build: Сборка Docker-образа и отправка его в Docker Hub.
4. Deploy: * Копирование конфигураций (включая nginx.conf и docker-compose.yml) на сервер.
    - Генерация файла .env из секретов GitHub.
    - Перезапуск контейнеров командой docker compose up -d --build.

### Ручное управление на сервере

1. Просмотр логов
   `docker compose logs`
2. Перезапуск проекта
   `docker compose up -d --build`
